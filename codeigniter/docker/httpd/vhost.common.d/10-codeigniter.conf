<Directory "/app">
    AllowOverride All
    Options FollowSymLinks
    Require all granted
</Directory>

# Forward Authorization headers so PHP can read them
SetEnvIf Authorization "(.*)" HTTP_AUTHORIZATION=$1
